import { ReactP5Wrapper } from "react-p5-wrapper";

const Background = () => {
    const sketch = (p5) =>{

        p5.setup = () => {
            
        }
        p5.draw = () => {

        }
    }
    return (
        <ReactP5Wrapper sketch={sketch}/>
    )
}

export default Background
    